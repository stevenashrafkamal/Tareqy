// Explicit production environment — used when running `ng build --configuration production`
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:3000' // IMPORTANT: Replace with your deployed backend URL before going live
};

