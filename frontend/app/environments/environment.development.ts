// ── Development environment ───────────────────────────────────────────────────
// Automatically used by `ng serve` via angular.json fileReplacements.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000',   // Angular dev server → Express backend
};
