// ── Default environment (production build) ───────────────────────────────────
// angular.json swaps this file with environment.development.ts during `ng serve`
// and with environment.production.ts during `ng build --configuration production`.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:3000',   // IMPORTANT: Replace with your deployed backend URL (e.g., https://api.tareqy.com)
};

